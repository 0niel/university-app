import 'package:flutter/material.dart';

class LostAndFoundDetailsPage extends StatelessWidget {
  const LostAndFoundDetailsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
