name: connectivity_client
description: A client that reports the device's network connectivity type.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace
dependencies:
  connectivity_plus: ^6.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: 10.3.0
