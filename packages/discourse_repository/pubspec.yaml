name: discourse_repository
description: A repository that exposes Discourse API endpoints.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  equatable: ^2.0.3
  discourse_api_client:
    path: ../discourse_api_client

dev_dependencies:
  coverage: ^1.2.0
  mocktail: ^1.0.0
  test: ^1.21.4
  very_good_analysis: ^6.0.0
