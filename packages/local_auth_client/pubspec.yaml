name: local_auth_client
description: A client that wraps device biometric authentication (local_auth).
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: 3.1.0
  local_auth: ^2.3.0

dev_dependencies:
  build_runner: 2.15.1
  flutter_test:
    sdk: flutter
  freezed: 3.2.5
  mocktail: ^1.0.4
  plugin_platform_interface: ^2.1.8
  very_good_analysis: 10.3.0
