/// Real student study groups — ownership, membership and invites via Supabase.
///
/// Replaces the unsafe "everyone who typed the same academic-group string is a
/// groupmate" model with first-class study group entities the group-space
/// mechanics scope to.
library;

export 'src/models/models.dart';
export 'src/study_groups_repository.dart';
