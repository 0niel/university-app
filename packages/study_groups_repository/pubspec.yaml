name: study_groups_repository
description: Real student study groups (ownership, membership, invites) via Supabase.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  equatable: ^2.0.7
  freezed_annotation: 3.1.0
  json_annotation: 4.12.0
  supabase: ^2.10.6

dev_dependencies:
  build_runner: 2.15.1
  freezed: 3.2.5
  http: ^1.6.0
  json_serializable: 6.14.0
  test: ^1.19.2
  very_good_analysis: 10.3.0
