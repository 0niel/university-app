include: ../../../analysis_options.yaml

dcm:
  rules:
    - prefer-match-file-name:
        exclude:
          - test/**
          - lib/src/auth_client.dart
    - no-empty-block:
        exclude:
          - test/**
