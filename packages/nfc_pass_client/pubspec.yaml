name: nfc_pass_client
description: attendance-app.mirea.client for Protobuf NFC API
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter
  grpc: ^4.0.1
  http: ^1.2.2
  protobuf: ^3.1.0
  random_user_agents: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0
