name: local_notifications_client
description: A client that schedules local device notifications (lesson reminders).
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.7.0 <4.0.0'

resolution: workspace

dependencies:
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_local_notifications: ^20.1.0
  flutter_timezone: ^5.1.0
  freezed_annotation: 3.1.0
  timezone: ^0.10.0

dev_dependencies:
  build_runner: 2.15.1
  flutter_test:
    sdk: flutter
  freezed: 3.2.5
  mocktail: ^1.0.0
  very_good_analysis: 10.3.0
